#!/bin/sh

# no gui or sets extraneous to maintaining my slackbuilds
slackpkg -default_answer=yes -batch=on install a ap d k l n tcl

# full install
#slackpkg -default_answer=yes -batch=on install e f kde t x xap y
